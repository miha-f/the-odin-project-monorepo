const knightMoves = (from, to) => {
    const GRID_SIZE = 8;

    const dirs = [
        [-1, 2], [-1, -2], [-2, 1], [-2, -1],
        [1, 2], [1, -2], [2, 1], [2, -1]
    ];

    const dfs = () => {
    };

};

export { knightMoves };
