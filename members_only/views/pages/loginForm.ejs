<div class="max-w-screen-xl mx-auto px-4 py-8">
    <form 
        class="space-y-4" 
        action="/users/login"
        method="POST"
    >
    
        <label for="email" class="text-text text-sm font-light">Email</label>
        <input
            type="email"
            id="email"
            name="email"
            placeholder="Email"
            value="<%= formData ? formData.email: '' %>"
            class="text-text w-full p-2 border border-gray-300 rounded"
        />
        <% if (formErrors?.email) { %>
        <ul class="text-text text-accent text-sm">
            <% formErrors.email.forEach(msg => { %>
            <li><%= msg %></li>
            <% }) %>
        </ul>
        <% } %>

        <label for="Password" class="text-text text-sm font-light">Password</label>
        <input
            type="password"
            name="password"
            min=5
            id="password"
            placeholder="Password"
            value="<%= formData ? formData.password: '' %>"
            class="text-text w-full p-2 border border-gray-300 rounded"
        />
        <% if (formErrors?.password) { %>
        <ul class="text-text text-accent text-sm">
            <% formErrors.password.forEach(msg => { %>
            <li><%= msg %></li>
            <% }) %>
        </ul>
        <% } %>

        <button
            type="submit"
            class="text-white px-4 py-2 bg-primary rounded hover:underline"
        >
            <%= userId ? 'Update' : 'Login' %>
        </button>
    </form>
</div>
