const LinkedList = () => {
    const append = (value) => { };
    const prepend = (value) => { };
    const size = () => { };
    const head = () => { };
    const tail = () => { };
    const at = (index) => { };
    const pop = () => { };
    const contains = (value) => { };
    const find = (value) => { };
    const toString = () => { };
    const insertAt = (value, index) => { };
    const removeAt = (index) => { };
}

const Node = (value = null, next = null) => {
    const value = value;
    const next = next;
    return { value, next };
}
