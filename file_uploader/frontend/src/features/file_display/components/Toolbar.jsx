// TODO(miha): Delete out of components, move BreadCrumbs to components


const Toolbar = () => {
    return (
        <div className="flex flex-row">
            <p>New folder</p>
            <p>Upload files</p>
        </div>
    );
};

export default Toolbar;
